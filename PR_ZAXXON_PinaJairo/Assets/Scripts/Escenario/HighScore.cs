using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class HighScore : MonoBehaviour
{/*
    public TextMeshProUGUI highscore;


    [SerializeField] GameObject otroObjeto;
    private Variables_Publicas variables_Publicas;
    int puntuacion;
    // Start is called before the first frame update
    void Start()
    {
        variables_Publicas = otroObjeto.GetComponent<Variables_Publicas>();
        highscore.text = PlayerPrefs.GetInt("HighScore", 0).ToString();
    }

    // Update is called once per frame
    void Update()
    {
        puntuacion = variables_Publicas.puntuacion;
    }
    void SetHighScore()
    {
        if(puntuacion > PlayerPrefs.GetInt("HighScore", 0))
        {
            PlayerPrefs.SetInt("HighScore", puntuacion);
            highscore.text = puntuacion.ToString();
        }
    }
    */
}
